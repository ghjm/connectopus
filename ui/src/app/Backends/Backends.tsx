import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const Backends: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">
      Backends
    </Title>
  </PageSection>
);

export { Backends };
